📶 TCP-Tweaker 1.0 (TCP-Speed)

TCP-Tweaker 1.0 es un script experimental diseñado para aplicar ajustes avanzados en la configuración de red de sistemas Linux, con el objetivo de optimizar la velocidad, estabilidad y rendimiento de conexiones Proxy, VPN o túneles SSH.

El script realiza modificaciones en parámetros del stack TCP, como buffers, tiempo de espera y control de congestión, buscando reducir la latencia y mejorar la experiencia de navegación. 

🛠️ Si los cambios no generan mejoras, simplemente vuelve a ejecutar `./tcptweaker.sh` para revertirlos automáticamente.

✔️ Características

- Optimización automática de parámetros TCP.
- Mejora potencial del rendimiento de red en entornos proxy, VPN o SSH.
- Reversible: ejecuta nuevamente para restaurar la configuración anterior.
- Compatible con la mayoría de distribuciones Linux.

⚠️ Nota: Es una herramienta experimental. Se recomienda probarla en entornos de prueba o realizar una copia de seguridad antes de aplicarla en producción.

👥 Créditos

- Repositorio: TCP-Tweaker 1.0 (TCP-Speed)
- Colaboradores y soporte:
  - https://t.me/Phreaker56
